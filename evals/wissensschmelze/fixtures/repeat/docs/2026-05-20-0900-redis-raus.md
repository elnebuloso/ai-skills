# Redis-Queue ersetzt (Mai)

Die Redis-Queue aus dem April-Stand ist raus. Wir nutzen jetzt PostgreSQL selbst
als Queue (SELECT ... FOR UPDATE SKIP LOCKED), um eine Komponente zu sparen.
Redis ist damit kein Teil der Architektur mehr.
